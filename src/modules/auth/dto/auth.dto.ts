 // IMPORTANT NOTE: classes are used instead of interfaces as TypeScript uses interfaces purely for type checking during development and compilation . Once the code is compiled down to standard JavaScript, the interfaces are removed entirely. They do not exist at runtime
// export class LoginDto {
//       email: string;
//       password: string;
//       ipAddress?: string;
//       userAgent?: string;
// }

// export class LoginResponseDto {
//       accessToken: string;
//       refreshToken: string;
// }